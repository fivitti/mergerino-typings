{
  "name": "mergerino-typing",
  "version": "1.0.0",
  "description": "TypeScript typing for Mergerino",
  "scripts": {
    "build": "tsc",
    "clear": "rimraf ./js",
    "start": "npm run clear && npm run lint && npm run build && npm test && npm run clear",
    "test": "ospec ./js/**/*.spec.js",
    "lint": "tslint --project ./tsconfig.json && dtslint ./typings/mergerino"
  },
  "author": "Fivitti",
  "license": "MIT",
  "dependencies": {
    "dtslint": "^0.9.1",
    "mergerino": "^0.4.0",
    "ospec": "^4.0.0",
    "rimraf": "^3.0.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
